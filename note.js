

/* note for REACT
component: function (starts with capital letter) returns html (lowercase)
- never nest function
- insert JSX using {} 
function HelloWorld() {
  return <div>{Math.ceil(3.6)}</div>;
}

images:
- require (for local images)
- uri (web images)

props/property:
<Post username="aakitchensinkk" /> 
<Post username="0205_kst" />
<Post username="forkangmo" />
- different properties of 'Post'

button:
<Button onPress={() => {
    // handle button press logic here
    console.log("Button Pressed!");
}}>
*/